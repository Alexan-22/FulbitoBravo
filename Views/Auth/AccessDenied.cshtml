@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="fb-auth-wrapper">
    <div class="fb-auth-card text-center">
        <div class="fb-auth-icon mx-auto" style="background:#d64550;color:#fff;"><i class="bi bi-slash-circle"></i></div>
        <h2 class="mb-2">Acceso denegado</h2>
        <p class="text-muted">No tienes permisos para ver esta sección.</p>
        <a class="btn btn-primary mt-2" asp-controller="Home" asp-action="Index">Volver al inicio</a>
    </div>
</div>
