@model IEnumerable<FulbitoBravo.Models.ReservaViewModel>

@{
    ViewData["Title"] = "Listado de Reservas";
    int paginaActual = ViewBag.PaginaActual ?? 1;
    int totalPaginas = ViewBag.TotalPaginas ?? 1;
}

<div class="page-card">
    <h2><i class="bi bi-calendar-check me-2"></i>Listado de Reservas</h2>

    <div class="mb-3">
        <a asp-action="Registrar" class="btn btn-success me-2">Nueva Reserva</a>
        <a asp-action="Reporte" class="btn btn-outline-primary">
            Reporte por Fechas
        </a>
    </div>

    <table class="table table-striped table-bordered table-hover">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Cliente</th>
                <th>Cancha</th>
                <th>Fecha</th>
                <th>Horario</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IdReserva</td>
                        <td>@item.NombreCliente</td>
                        <td>@item.NombreCancha</td>
                        <td>@item.FechaReserva.ToString("dd/MM/yyyy")</td>
                        <td>@item.HorarioTexto</td>
                        <td>
                            @if (item.EstadoReserva == "Confirmada")
                            {
                                <span class="badge bg-success">@item.EstadoReserva</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">@item.EstadoReserva</span>
                            }
                        </td>
                        <td>
                            @if (item.FechaReserva.Date >= DateTime.Today)
                            {
                                <a asp-action="EditarEstado" asp-route-id="@item.IdReserva" class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-pencil"></i> Estado
                                </a>
                            }
                            else
                            {
                                <span class="text-muted small">—</span>
                            }
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="6" class="text-center text-muted">No se encontraron reservas.</td>
                </tr>
            }
        </tbody>
    </table>

    @* ====== CONTROLES DE PAGINACIÓN ====== *@
    @if (totalPaginas > 1)
    {
        <nav aria-label="Paginación de reservas">
            <ul class="pagination justify-content-center">

                @* Botón Anterior *@
                <li class="page-item @(paginaActual <= 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="Index" asp-route-pagina="@(paginaActual - 1)">
                        Anterior
                    </a>
                </li>

                @* Números de página *@
                @for (int i = 1; i <= totalPaginas; i++)
                {
                    <li class="page-item @(i == paginaActual ? "active" : "")">
                        <a class="page-link" asp-action="Index" asp-route-pagina="@i">
                            @i
                        </a>
                    </li>
                }

                @* Botón Siguiente *@
                <li class="page-item @(paginaActual >= totalPaginas ? "disabled" : "")">
                    <a class="page-link" asp-action="Index" asp-route-pagina="@(paginaActual + 1)">
                        Siguiente
                    </a>
                </li>

            </ul>
        </nav>

        <p class="text-center text-muted">
            Página @paginaActual de @totalPaginas 
            (Total: @ViewBag.TotalRegistros registros)
        </p>
    }
</div>